<div class="space-y-4">
  <div role="tablist" class="tabs tabs-border">
    <% tabs.each do |tab| %>
      <%= link_to tab[:label], tab[:path],
      class: "tab #{current_page?(tab[:path]) ? 'tab-active' : ''}" %>
    <% end %>
  </div>
</div>
