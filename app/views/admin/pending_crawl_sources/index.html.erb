<div class="space-y-6">
  <div class="flex justify-between items-center">
    <div>
      <h1 class="text-2xl font-bold tracking-tight">Pending Crawl Sources</h1>
      <p class="text-gray-600">Approve or reject new data sources</p>
    </div>
  </div>

  <div class="space-y-4">
    <%= render 'shared/crawl_sources_tab' %>
    <%= render 'pending', crawl_sources: @crawl_sources  %>
  </div>
</div>
