<div class="space-y-6">
  <div class="flex justify-between items-center">
    <div>
      <h1 class="text-2xl font-bold tracking-tight">Pending Crawl Sources</h1>
      <p class="text-gray-600">Approve or reject new data sources</p>
    </div>
  </div>

  <div class="space-y-4">
    <%= render partial: "shared/tabs", locals: {
      tabs: [
        { label: "Data Sources", path: admin_crawl_sources_path },
        { label: "Pending Approval", path: admin_pending_crawl_sources_path },
        { label: "Crawl History", path: admin_history_crawl_sources_path }
      ]
    } %>
    <%= render 'pending', crawl_sources: @crawl_sources  %>
  </div>
</div>
